services:
  fasttracker:
    build: .
    ports:
      - "5000:5000"
    restart: unless-stopped
    working_dir: /app
    volumes:
      - ./run_history:/app/run_history
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
